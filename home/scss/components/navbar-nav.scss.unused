@use '../util'as *;

@use '~bootstrap/scss/bootstrap';

@use '~bootstrap/scss/bootstrap';

// Override Bootstrap's styles for nav items
.navbar-nav {
    .nav-link {
        // Change the color of nav links
        color: red; // Change to your desired color

        // Change the font size of nav links
        font-size: rem(50); // Change to your desired font size
    }
}